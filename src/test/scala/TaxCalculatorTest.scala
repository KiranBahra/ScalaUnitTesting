import org.scalatest._

/*
 "add" should "add numbers" in {
    val nums = new TwoNumbers(1, 2)
    assert(nums.add === 3)



If the input greater than 10,000, instead return 15% of the total value
If the input is greater than 50,000, instead return 20% of the total value
If the input is greater than 100,000, instead return 40% of the total value

 */
/*
class TaxCalculatorSpec extends FlatSpec {
  "calculateTax" should "calculate tax for 10000 and return 0" in {
    val test1 = new TaxCalculate()
    assert(test1.calculateTax(10000) === 0)
  }
  "calculateTax" should "calculate tax for 10001 and return 1500" in {
    val test2 = new TaxCalculate()
    assert(test2.calculateTax(10001) === 1500)
  }
  "calculateTax" should "calculate tax for 50001 and return 7500" in {
    val test2 = new TaxCalculate()
    assert(test2.calculateTax(50001) === 7500)
  }
}*/